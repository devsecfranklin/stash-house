# www

This is a go lang app to generate dynamic web pages.

```sh
go get github.com/mattn/go-sqlite3
/usr/local/go/bin/go run main.go
sudo /usr/sbin/nginx -t # validate the nginx config
```

## setup database
