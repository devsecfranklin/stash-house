{{define "mcAdminPage"}}

<!DOCTYPE html>
<html>

<head>
    <meta name="generator" content="HTML Tidy for HTML5 for Linux version 5.6.0">
    <meta name="orcid" content="https://orcid.org/0000-0003-4586-8500">
    <title>{{.Title}}</title>
    <link rel="stylesheet" type="text/css" href="/web/css/puzzle.css" />
</head>

<body>

<a href="https://github.com/devsecfranklin/game-server-minecraft">This is the Github repo where the game files are stored.</a>

<h1>Mapping</h1>

We use the &quot;Unmined&quot; software to map the explored areas of the server.

<p>
There is a shell script `/home/minecraft/bin/unmined.sh` that can be used to generate the map files.<br />
It writes output to `/var/www/html/` unless you change the default.
</p>

<h2 id="usage">Usage</h2>
<pre><code>unmined-cli &lt;module&gt; &lt;verb&gt; [options]</code></pre>
<p>Available modules: image, web</p>
<p>Show module verbs:</p>
<pre><code>unmined-cli &lt;module&gt; help</code></pre>
<p>Show verb options:</p>
<pre><code>unmined-cli &lt;module&gt; help &lt;verb&gt;</code></pre>
<h2 id="examples">Examples</h2>
<p>Show available options for verb “render” in module “image”:</p>
<pre><code>unmined-cli image help render</code></pre>
<p>Render a single image file at zoom level -3 (1 pixel = 2^3 = 8
blocks):</p>
<pre><code>unmined-cli image render --zoom=-3 --trim --world=&quot;%APPDATA%\.minecraft\saves\My World&quot; --output=&quot;%USERPROFILE%\Desktop\MyMap.png&quot;</code></pre>
<p>Render a web map:</p>
<pre><code>unmined-cli web render --world=&quot;%APPDATA%\.minecraft\saves\My World&quot; --output=&quot;%USERPROFILE%\Desktop\MyMap&quot;</code></pre>
<h2 id="parameters">Parameters</h2>
<h3 id="area">–area</h3>
<p>Syntax of the –area parameter:</p>
<p>Selecting a rectangle using x,z coordinates and width/height:</p>
<pre><code>--area=r(x,z,width,height)
--area=c(x,z,width,height)
--area=b(x,z,width,height)</code></pre>
<p>Selecting a rectangle using x1,z1 and x2,z2 coordinates:</p>
<pre><code>--area=r((x1,z1),(x2,z2))
--area=c((x1,z1),(x2,z2))
--area=b((x1,z1),(x2,z2))</code></pre>
<p>Meaning of the first character:</p>
<ul>
<li>r = region coordinates</li>
<li>c = chunk coordinates</li>
<li>b = block coordinates</li>
</ul>
<p>Coordinate math:</p>
<ul>
<li>(x1,z1) and (x2,z2) points are inclusive (inside the rectangle)</li>
<li>width = x2 - x1</li>
<li>height = z2 - z1</li>
<li>1 chunk = 16 blocks</li>
<li>1 region = 32 chunks = 512 blocks</li>
</ul>
<h4 id="examples-1">Examples:</h4>
<p>Select 100x50 blocks area at x=2000, z=2000:</p>
<pre><code>--area=b(2000,2000,100,50)
--area=b((2000,2000),(2101,2051))</code></pre>
<p>Select one region at x=2, z=2 (r.2.2.mca):</p>
<pre><code>--area=r(2,2,1,1)
--area=c(64,64,32,32)
--area=b(1024,1024,512,512)

--area=r((2,2),(3,3))
--area=c((64,64),(97,97))
--area=b((1024,1024),(1537,1537))</code></pre>

<ol>
<li>This server has been running continuously since 2012.</li>
<li>Note that this is a survival server, not a creative server.</li>
<li>Please don't waste time begging for admins to toggle your gamemode.</li>
</ol>

<h1>mods</h1>

<h2>Automodpack</h2>

<p>/amp is an alias for /automodpack.<br />Certificate fingerprint: b7682adb708af6b47ae5c4ecb54f6b46af34b67ffa0ade00cb450b6109f7b851</p>

<table>
<tr><td>Command</td><td>description</td></tr>
<tr><td>/automodpack</td><td>Status of automodpack and general help</td></tr>
<tr><td>/automodpack generate</td><td>Generate modpack</td></tr>
<tr><td>/automodpack host</td><td>Status of modpack hosting</td></tr>
<tr><td>/automodpack host start</td><td>Start modpack hosting</td></tr>
<tr><td>/automodpack host stop</td><td>Stop modpack hosting</td></tr>
<tr><td>/automodpack host restart</td><td>Restart modpack hosting</td></tr>
<tr><td>/automodpack host connections</td><td>Lists all currently active modpack host connections</td></tr>
<tr><td>/automodpack host fingerprint</td><td>Get the certificate fingerprint of the modpack host</td></tr>
<tr><td>/automodpack config reload</td><td>Reload config files</td></tr>
</table>

<h2>Botania</h2>

Botania is a mod centered around generating and harnessing the magical energy produced by mystical flowers. Botania's documentation book, the Lexica Botania, displays crafting recipes and explains nearly everything about Botania in-game.<br />

<h2>Modnomicon</h2>

<p>
This is <a href="https://github.com/klikli-dev/modonomicon">Data-driven minecraft in-game documentation with progress visualization</a>
</p>

<h2>Occultism</h2>

The first goal is to obtain the Dictionary of Spirits, an in-game guide with information about all spirits,
rituals and pentacles, as well as an in-depth getting started guide.<br />
Demon's Dream seeds are seeds used to grow the Demon's Dream fruits. They are a rare drop from grass, similar to vanilla Wheat seeds.<br />

<ol>
<li>Obtain Demon's Dream Seeds by breaking grass. Keep one and plant a few others.</li>
<li>Combine the seeds with a book to create the dictionary</li>
<li>Read the Getting Started section of the book to learn about the Third Eye, Demon's Dream, and Spiritfire</li>
<li>Make a stick of Chalk, clear out some floor space, and start drawing some Pentacles</li>
<li>Conduct your first ritual and meet a demon</li>
</ol>

<h2>Thermal Series</h2>

Thermal Innovation<br />

<h1>AI stuff</h1>


{{template "footer"}}

</body>
</html>
{{end}}
